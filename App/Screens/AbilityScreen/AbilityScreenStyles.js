import { StyleSheet } from "react-native";
const style = StyleSheet.create({
  name: {
    fontSize: 14,
    color: "white"
  },
  pokedexText: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 10,
    marginRight: 10,
    textAlign: "right"
  }
});
export default style;
